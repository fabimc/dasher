{
  "routes": [
    {
      "method": "GET",
      "path": "/promotions",
      "handler": "Promotion.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/promotions/count",
      "handler": "Promotion.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/promotions/:id",
      "handler": "Promotion.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/promotions",
      "handler": "Promotion.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/promotions/:id",
      "handler": "Promotion.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/promotions/:id",
      "handler": "Promotion.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
